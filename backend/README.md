# Backend

FastAPI and LangGraph application scaffold for the DeepAgent Sandbox PoC.

This directory will eventually contain:

- the product-facing backend API
- the LangGraph deployment app
- Postgres-backed metadata models
- MinIO integration
- the custom DeepAgent sandbox backend

For now, this is a Phase 2 skeleton that establishes the package layout, configuration model, core routes, and service boundaries.
